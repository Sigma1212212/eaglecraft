# Runtime data — regenerated by setup.sh / at runtime
/data/
/runtime/
/logs/

# Python
__pycache__/
*.pyc

# Downloads / archives
*.tar.gz
*.jar

# OS
.DS_Store

# NOTE: web/eaglercraft/index.html (the game client) IS committed on purpose
# so the repo is self-contained. smp-config/ and bungee-config/ are committed.

# games are cloned from Gams-Offline/Gams by setup.sh (1.6GB, not committed)
/web/games/
