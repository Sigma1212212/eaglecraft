# Vanilla SMP: no permission plugin, no economy, no sign shops.
#
# Everything players can do is plain vanilla Minecraft. Operators are
# granted with the vanilla `op <name>` console command (ops.json), which
# is all a survival server needs.
#
# This file is intentionally empty. Adding nodes here only matters if you
# later install a Bukkit plugin that declares permissions.
